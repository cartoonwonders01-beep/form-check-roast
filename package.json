{
  "name": "form-check-roast",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && rm -rf dist && cp -r client/dist ./dist",
    "dev": "npm --prefix client run dev"
  }
}
